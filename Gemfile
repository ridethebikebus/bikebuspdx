source 'https://rubygems.org'

gem "jekyll", "~> 4.3.2"
gem "webrick" # required when using Ruby >= 3 and Jekyll <= 4.2.2
gem "jekyll-paginate"
gem 'jekyll-tagging'
gem "jekyll-sitemap"
gem "sass-embedded", "< 1.70.0" # Avoid deprecation warnings in code we aren't updating anyway.
